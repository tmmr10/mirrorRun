enum PlayState { menu, countdown, playing, dead }

enum ObstacleType { wall, spike, doubleWall, shifter }

enum BiomeType { forest, city, crystal, volcano, desert, ocean, ruins, space, storm, neon, void_ }
